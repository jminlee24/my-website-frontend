const Background = () => {
  return (
    <div className="absolute bg-fixed w-full h-full bg-gray-950 z-0 bg-gradient-to-b from-black to-gray-600">
      Background
    </div>
  );
};

export default Background;
